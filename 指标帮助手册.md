## fill_na()
判断每个数据点是否为空值，若为空值则按照指定方式（forward或backward）填充

### 句法
```python
fill_na(x, method) -> Sequence[float, int, bool]
```

### 参数
**x (Sequence([float, int, bool]))** 数据```Sequence```，必填参数  
**method (str)** 填充方法，可选参数，默认值为forward，可能的值为'forward','backward'

---

## indicator()
定义指标的名称、简称、展示位置、简介等

### 句法
```python
indicator(short_name, name, main_chart, remarks)
```

### 参数
**short_name (str)** 指标缩写，必填参数  
**name (str)** 指标全名，必填参数  
**main_chart (bool)** 是否为主图指标，可选参数，默认值为False  
**remarks (str)** 指标介绍，可选参数，默认值为''

---

## input_parameter()
可进行参数、颜色、线型等的输入

### 句法
```python
input_parameter(title, default) -> str | int | float | bool | Color | Shape | Line
```

### 参数
**title (str)** 参数名，必填参数  
**default (str, int, float, bool)** 默认值，必填参数

---

## is_na()
判断每个数据点是否为空值，返回布尔值序列

### 句法
```python
is_na(x) -> Sequence[bool]
```

### 参数
**x (Sequence([float, int, bool]), float, int, bool)** 数据，必填参数

---

## max()
返回多个数或数列的最大值

### 句法
```python
max(arg1, arg2, /, *args) -> Sequence[float, int] | float | int
```

### 参数
**arg1 (Sequence([float, int]), float, int)** 待比较的实参，必填参数  
**arg2 (Sequence([float, int]), float, int)** ，必填参数

---

## min()
返回多个数或数列的最小值

### 句法
```python
min(arg1, arg2, /, *args) -> Sequence[float, int] | float | int
```

### 参数
**arg1 (Sequence([float, int]), float, int)** 待比较的实参，必填参数  
**arg2 (Sequence([float, int]), float, int)** ，必填参数

---

## ref()
引用n周期前的数据

### 句法
```python
ref(x, n) -> Sequence[float, int, bool]
```

### 参数
**x (Sequence([float, int, bool]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## replace_na()
判断每个数据点是否为空值，若为空值则填充为value

### 句法
```python
replace_na(x, value) -> Sequence[float, int, bool]
```

### 参数
**x (Sequence([float, int, bool]))** 数据```Sequence```，必填参数  
**value (int/float/bool)** 填充值，必填参数

---

## output_parameter()
声明用于指标回测的变量

### 句法
```python
output_parameter(**kwargs)
```

### 参数
****kwargs (Sequence([float, int, bool]))** 用于指标回测的参数，必填参数

---

## date_interval()
返回```Sequence```内index到目标日的天数，天数类型为int

### 句法
```python
date_interval(data, target_date) -> Sequence[int]
```

### 参数
**data (Sequence)** 数据```Sequence```，可选参数，默认值为当前图表默认展示的收盘价队列  
**target_date (str,datetime)** 目标日，可选参数，默认值为1970-01-01 00:00:00

---

## day()
返回```Sequence```内index的日期，日期类型为int

### 句法
```python
day(data) -> Sequence[int]
```

### 参数
**data (Sequence)** 数据```Sequence```，可选参数，默认值为当前图表默认展示的收盘价队列

---

## from_open()
返回目标当前时刻今日已开盘有多长分钟，分钟类型为int

### 句法
```python
from_open() -> float
```

---

## hour()
返回```Sequence```内index的小时数，小时数类型为int

### 句法
```python
hour(data) -> Sequence[int]
```

### 参数
**data (Sequence)** 数据```Sequence```，可选参数，默认值为当前图表默认展示的收盘价队列

---

## minute()
返回```Sequence```内index的分钟数，分钟数类型为int

### 句法
```python
minute(data) -> Sequence[int]
```

### 参数
**data (Sequence)** 数据```Sequence```，可选参数，默认值为当前图表默认展示的收盘价队列

---

## month()
返回```Sequence```内index的月份，月份类型为int

### 句法
```python
month(data) -> Sequence[int]
```

### 参数
**data (Sequence)** 数据```Sequence```，可选参数，默认值为当前图表默认展示的收盘价队列

---

## period()
返回目标的周期类型，参照```BarType```枚举类

### 句法
```python
period() -> BarType
```

---

## second()
返回```Sequence```内index的秒数，秒数类型为int

### 句法
```python
second(data) -> Sequence[int]
```

### 参数
**data (Sequence)** 数据```Sequence```，可选参数，默认值为当前图表默认展示的收盘价队列

---

## total_trading_minute()
返回目标每天的总交易分钟数，分钟数类型为int

### 句法
```python
total_trading_minute() -> Sequence[int]
```

---

## weekday()
返回```Sequence```内index是星期几，星期数类型为int

### 句法
```python
weekday(data) -> Sequence[int]
```

### 参数
**data (Sequence)** 数据```Sequence```，可选参数，默认值为当前图表默认展示的收盘价队列

---

## weekofyear()
返回```Sequence```内index所处周是年内第几个周，年内星期类型为int，年内第一个周一开始算作第一周，之前的日子算作第0周

### 句法
```python
weekofyear(data) -> Sequence[int]
```

### 参数
**data (Sequence)** 数据```Sequence```，可选参数，默认值为当前图表默认展示的收盘价队列

---

## year()
返回```Sequence```内index的年份，年份类型为int

### 句法
```python
year(data) -> Sequence[int]
```

### 参数
**data (Sequence)** 数据```Sequence```，可选参数，默认值为当前图表默认展示的收盘价队列

---

## acos()
计算反余弦值，返回弧度制

### 句法
```python
acos(x) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## asin()
计算反正弦值，返回弧度制

### 句法
```python
asin(x) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## atan()
计算反正切值，返回弧度制

### 句法
```python
atan(x) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## avedev()
计算过去n个数与其算数平均值偏差绝对值的平均

### 句法
```python
avedev(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## ceil()
返回 x 的向上取整，即大于或等于 x 的最小的整数

### 句法
```python
ceil(x) -> Sequence[int] | int
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## corr()
计算数据序列x和数据序列y过去n周期的相关系数

### 句法
```python
corr(x ,y, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```1，必填参数  
**y (Sequence([float, int]))** 数据```Sequence```2，必填参数  
**n (int)** 周期数，必填参数

---

## cos()
计算余弦值，返回弧度制

### 句法
```python
cos(x) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## covar()
计算数据序列x和数据序列y过去n个周期的协方差

### 句法
```python
covar(x ,y, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```1，必填参数  
**y (Sequence([float, int]))** 数据```Sequence```2，必填参数  
**n (int)** 周期数，必填参数

---

## devsq()
计算过去n个数与其算数平均值偏差的平方和

### 句法
```python
devsq(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## floor()
返回 x 的向下取整，即小于或等于 x 的最大的整数

### 句法
```python
floor(x) -> Sequence[int] | int
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## forcast()
以过去n个数与其对应顺序编号（从过去到现在第几根）进行带截距项的线性回归，通过拟合回归模型计算数据序列的下一期预测值

### 句法
```python
forcast(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## fracpart()
返回去除整数部分的 x ，只留下小数部分

### 句法
```python
fracpart(x) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## iv()
计算使期权价格等于市场价格的波动率

### 句法
```python
iv() -> Sequence[float]
```

---

## ln()
计算以自然常数为底的对数

### 句法
```python
ln(x) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## math_log()
计算以base为底的x的对数

### 句法
```python
math_log(x,base) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 真数，必填参数  
**base (int, float)** 底数，可选参数，默认值为10

---

## mod()
返回m除以n的余数

### 句法
```python
mod(m, n) -> Sequence[float, int] | float | int
```

---

## mular()
计算n周期中x的乘积，n=0则从第一个非空值开始计算

### 句法
```python
mular(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## rand()
如果n为浮点数或整数，则返回不大于n的随机正整数；如果n为```Sequence```，则对于n中的每个value生成一个不大于value的随机正整数，最终返回一个由随机数填充的```Sequence```

### 句法
```python
rand(n) -> Sequence[int] | int
```

### 参数
**n (Sequence([float, int]), float, int)** 随机取值上限，必填参数

---

## sign()
如果x>0,返回1；如果x<0,返回-1；如果x=0，返回0

### 句法
```python
sign(x) -> Sequence[int] | int
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## sin()
计算正弦值，返回弧度制

### 句法
```python
sin(x) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## slope()
以过去n个数与其对应顺序编号（从过去到现在第个数）进行带截距项的线性回归，获得回归模型对数据序列的拟合系数

### 句法
```python
slope(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## std()
计算过去n个数样本标准差

### 句法
```python
std(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## stdp()
计算过去n个数总体标准差

### 句法
```python
stdp(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## sum()
计算n周期中x的总和，n=0则从第一个非空值开始计算

### 句法
```python
sum(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## tan()
计算正切值，返回弧度制

### 句法
```python
tan(x) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## trunc()
返回去除小数部分的 x ，只留下整数部分

### 句法
```python
trunc(x) -> Sequence[int] | int
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## var()
计算过去n个数样本方差

### 句法
```python
var(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## varp()
计算过去n个数总体方差

### 句法
```python
varp(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## volat()
计算价格序列过去n个周期的年化波动率

### 句法
```python
volat(price, n, bar_type) -> Sequence[float]
```

### 参数
**price (Sequence([float, int]))** 价格序列，必填参数  
**n (int)** 周期数，必填参数  
**bar_type (BarType)** 频率，可选参数，默认值为None

---

## plot()
根据输入的序列绘制个性化的线型

### 句法
```python
plot(name, x, color, style, linewidth, ref)
```

### 参数
**name (str)** 名称，必填参数  
**x (Sequence([float, int, bool]), float, int, bool)** 绘图数据，必填参数  
**color (Color, Sequence(Color))** 画线颜色，可选参数，默认值为Color.blue  
**style (Line)** 线型，可选参数，默认值为Line.line  
**linewidth (int)** 线宽，可选参数，默认值为1，可能的值为1~9
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## plot_candlestick()
根据输入的开盘、收盘、最高、最低序列来绘制蜡烛图

### 句法
```python
plot_candlestick(name, high, open, low, close, ref)
```

### 参数
**name (str)** 名称，必填参数  
**high (Sequence([float, int]), float, int)** 最高价序列，必填参数  
**open (Sequence([float, int]), float, int)** 开盘价序列，必填参数  
**low (Sequence([float, int]), float, int)** 最低价序列，必填参数  
**close (Sequence([float, int]), float, int)** 收盘价序列，必填参数  
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## plot_fillcolor()
根据判断条件在val1和val2间填充颜色

### 句法
```python
plot_fillcolor(name, val1, val2, cond, color, ref)
```

### 参数
**name (str)** 名称，必填参数  
**val1 (Sequence([float, int]), float, int)** 绘图数据1，必填参数  
**val2 (Sequence([float, int]), float, int)** 绘图数据2，必填参数  
**cond (Bool, Sequence(bool))** 判断条件，必填参数  
**color (Color, Sequence(Color))** 画线颜色，可选参数，默认值为Color.blue  
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## plot_hline()
根据输入的数据绘制个性化的水平线

### 句法
```python
plot_hline(name, x, color, style, linewidth, ref)
```

### 参数
**name (str)** 名称，必填参数  
**x (float, int)** 绘图数据，必填参数  
**color (Color)** 画线颜色，可选参数，默认值为Color.gray  
**style (Line)** 线型，可选参数，默认值为Line.line_dashed  
**linewidth (int)** 线宽，可选参数，默认值为1，可能的值为1~9  
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## plot_icon()
根据判断条件在指定位置绘制个性化图标

### 句法
```python
plot_icon(name, cond, y, type, color, size, offseth, offsetv, ref)
```

### 参数
**name (str)** 名称，必填参数  
**cond (Bool, Sequence(bool))** 判断条件，必填参数  
**y (Sequence([float, int]), float, int)** 指定位置，必填参数  
**type (Shape)** 图标类型，必填参数  
**color (Color, Sequence(Color))** 画线颜色，可选参数，默认值为Color.blue  
**size (int)** 缩放因子，可选参数，默认值为1，可能的值为1~9  
**offseth (float)** 水平方向偏移度，可选参数，默认值为0  
**offsetv (float)** 垂直方向偏移度，可选参数，默认值为0  
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## plot_stickline()
根据判断条件在top到bottom之间添加个性化柱图

### 句法
```python
plot_stickline(name, cond,bottom,top,width,empty, color, ref)
```

### 参数
**name (str)** 名称，必填参数  
**cond (Bool, Sequence(bool))** 判断条件，必填参数  
**bottom (Sequence([float, int]), float, int)** 柱状图底部序列，必填参数  
**top (Sequence([float, int]), float, int)** 柱状图顶部序列，必填参数  
**width (int, float)** 柱状图宽度，可选参数，默认值为0.7，可能的值为0~9  
**empty (bool)** 是否为空心柱图，可选参数，默认值为True  
**dashed (bool)** 边框是否为虚线，可选参数，默认值为True  
**color (Color, Sequence(Color))** 画线颜色，可选参数，默认值为Color.blue  
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## plot_text()
根据判断条件在指定位置添加个性化文字

### 句法
```python
plot_text(name, cond, y, text, color, size, offseth, offsetv, ref)
```

### 参数
**name (str)** 名称，必填参数  
**cond (Bool, Sequence(bool))** 判断条件，必填参数  
**y (Sequence([float, int]), float, int)** 指定位置，必填参数  
**text (str)** 指定文字，必填参数  
**color (Color, Sequence(Color))** 画线颜色，可选参数，默认值为Color.blue  
**size (int)** 字体大小，可选参数，默认值为1，可能的值为1~9  
**offseth (float)** 水平方向偏移度，可选参数，默认值为0  
**offsetv (float)** 垂直方向偏移度，可选参数，默认值为0  
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## amount()
返回目标成交额序列

### 句法
```python
amount(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## capital()
返回目标当前最新的流通股本（手）

### 句法
```python
capital() -> float
```

---

## close()
返回目标收盘价序列

### 句法
```python
close(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## high()
返回目标最高价序列

### 句法
```python
high(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## low()
返回目标最低价序列

### 句法
```python
low(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## open()
返回目标开盘价序列

### 句法
```python
open(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## total_amount()
返回目标当天累计成交额序列

### 句法
```python
total_amount(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## total_vol()
返回目标当天累计成交量序列

### 句法
```python
total_vol(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## vol()
返回目标成交量序列

### 句法
```python
vol(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## vola()
返回目标成交量序列，若目标没有成交量数据，则返回成交额序列

### 句法
```python
vola(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## money_net_inflow()
返回目标类型的资金净流入数据序列

### 句法
```python
money_net_inflow(type, ref) -> Sequence(float)
```

### 参数
**type (MoneyFlowType，str)** 资金流向类型，可选参数，默认值为MoneyFlowType.large，可能的值为extra_large,large,medium,small,MoneyFlowType.extra_large,MoneyFlowType.large,MoneyFlowType.medium,MoneyFlowType.small  
**ref (int)** 引用n周期前的数据，可选参数，默认值为0

---

## pe_ratio()
返回目标市盈率序列

### 句法
```python
pe_ratio(ref) -> Sequence(float)
```

### 参数
**ref (int)** 引用n周期前的数据，可选参数，默认值为0

---

## turnover_rate()
返回目标换手率序列

### 句法
```python
turnover_rate(ref) -> Sequence(float)
```

### 参数
**ref (int)** 引用n周期前的数据，可选参数，默认值为0

---

## cross()
前n个点x均小于y，当前点x大于y, 则视为上穿，否则为不上穿

### 句法
```python
cross(x, y, n) -> Sequence[bool]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```1，必填参数  
**y (Sequence([float, int]))** 数据```Sequence```2，必填参数  
**n (int)** 周期数，必填参数

---

## dma()
计算动态移动平均

### 句法
```python
dma(x, m) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**m (float)** 权重，必填参数

---

## down_n_day()
过去n周期内的value均递减，则视为连跌，否则为不连跌

### 句法
```python
down_n_day(x, n) -> Sequence[bool]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## ema()
计算指数移动平均

### 句法
```python
ema(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，可选参数，默认值为5

---

## exist()
过去n周期内的x至少出现过一个True，则视为存在，否则为不存在

### 句法
```python
exist(x, n) -> Sequence[bool]
```

### 参数
**x (Sequence(bool))** 状态序列，必填参数  
**n (int)** 周期数，必填参数

---

## greater_n_day()
过去n周期内的x均大于y，则视为连大，否则为不连大

### 句法
```python
greater_n_day(x, y, n) -> Sequence[bool]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```1，必填参数  
**y (Sequence([float, int]))** 数据```Sequence```2，必填参数  
**n (int)** 周期数，必填参数

---

## hhv()
求n周期内的最高值

### 句法
```python
hhv(x, n, min_period) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## hhv_bars()
对每一个数据点，返回n周期内最高值所在位置到当前数据点的周期数，若有多个相等的最高值，则返回距离当前数据点最近的位置

### 句法
```python
hhv_bars(x, n, min_period) -> Sequence[int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## hod()
返回当前数据是n周期内的第几个高值

### 句法
```python
hod(x, n, min_period) -> Sequence[int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## last()
在step步长前n周期内x一直为True，则视为持续，否则为不持续

### 句法
```python
last(x, step, n) -> Sequence[bool]
```

### 参数
**x (Sequence(bool))** 状态序列，必填参数  
**step (int)** 步长，必填参数  
**n (int)** 周期数，必填参数

---

## llv()
求n周期内的最低值

### 句法
```python
llv(x, n, min_period) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## llv_bars()
对每一个数据点，返回n周期内最低值所在位置到当前数据点的周期数，若有多个相等的最低值，则返回距离当前数据点最近的位置

### 句法
```python
llv_bars(x, n, min_period) -> Sequence[int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## lod()
返回当前数据是n周期内的第几个低值

### 句法
```python
lod(x, n, min_period) -> Sequence[int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## reverse()
计算相反数

### 句法
```python
reverse(x) -> Sequence[float, int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数

---

## sma()
计算简单移动平均

### 句法
```python
sma(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，可选参数，默认值为5

---

## smma()
计算平滑移动平均

### 句法
```python
smma(x, n, m) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，可选参数，默认值为5  
**m (float)** 权重，可选参数，默认值为1

---

## tma()
计算递归移动平均

### 句法
```python
tma(x, a, b) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**a (float)** 权重，可选参数，默认值为0.5  
**b (float)** 权重，可选参数，默认值为0.5

---

## tr()
根据前收价和当时最高最低价计算每一时间点的真实波幅，计算公式为max(当前时间点的最高价，前一时间点的收盘价)-min(当前时间点的最低价，前一时间点的收盘价)

### 句法
```python
tr(ref) -> Sequence[float]
```

### 参数
**ref (int)** 需要移位的周期数，可选参数，默认值为0

---

## up_n_day()
过去n周期内的value均递增，则视为连涨，否则为不连涨

### 句法
```python
up_n_day(x, n) -> Sequence[bool]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，必填参数

---

## wma()
计算加权移动平均

### 句法
```python
wma(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，可选参数，默认值为5

---

## xma()
计算偏移移动平均

### 句法
```python
xma(x, n) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**n (int)** 周期数，可选参数，默认值为5

---

## rise_count()
返回指定指数上涨股票个数序列

### 句法
```python
rise_count(index) -> Sequence(int)
```

### 参数
**index (str)** 目标指数，必填参数，可能的值为'HSI', 'HSTECH', 'SPX', 'DJI', 'IXIC', 'NDX', 'SOX', 'N225', 'HXC'

| 枚举值 | 含义 |
|--------|------|
| HSI | 恒生指数 |
| HSTECH | 恒生科技指数 |
| SPX | 标普500指数 |
| DJI | 道琼斯指数 |
| IXIC | 纳斯达克综合指数 |
| NDX | 纳斯达克100指数 |
| SOX | 费城半导体指数 |
| N225 | 日经225指数 |
| HXC | 纳斯达克中国金龙指数 |

---

## fall_count()
返回指定指数下跌股票个数序列

### 句法
```python
fall_count(index) -> Sequence(int)
```

### 参数
**index (str)** 目标指数，必填参数，可能的值为'HSI', 'HSTECH', 'SPX', 'DJI', 'IXIC', 'NDX', 'SOX', 'N225', 'HXC'

| 枚举值 | 含义 |
|--------|------|
| HSI | 恒生指数 |
| HSTECH | 恒生科技指数 |
| SPX | 标普500指数 |
| DJI | 道琼斯指数 |
| IXIC | 纳斯达克综合指数 |
| NDX | 纳斯达克100指数 |
| SOX | 费城半导体指数 |
| N225 | 日经225指数 |
| HXC | 纳斯达克中国金龙指数 |

---

## component_count()
返回指定指数成分股数量序列

### 句法
```python
component_count(index) -> Sequence(int)
```

### 参数
**index (str)** 目标指数，必填参数，可能的值为'HSI', 'HSTECH', 'SPX', 'DJI', 'IXIC', 'NDX', 'SOX', 'N225', 'HXC'

| 枚举值 | 含义 |
|--------|------|
| HSI | 恒生指数 |
| HSTECH | 恒生科技指数 |
| SPX | 标普500指数 |
| DJI | 道琼斯指数 |
| IXIC | 纳斯达克综合指数 |
| NDX | 纳斯达克100指数 |
| SOX | 费城半导体指数 |
| N225 | 日经225指数 |
| HXC | 纳斯达克中国金龙指数 |

---

## over_ma200()
返回指定指数成分股大于MA200个数序列

### 句法
```python
over_ma200(index) -> Sequence(int)
```

### 参数
**index (str)** 目标指数，必填参数，可能的值为'HSI', 'HSTECH', 'SPX', 'DJI', 'IXIC', 'NDX', 'SOX', 'N225', 'HXC'

| 枚举值 | 含义 |
|--------|------|
| HSI | 恒生指数 |
| HSTECH | 恒生科技指数 |
| SPX | 标普500指数 |
| DJI | 道琼斯指数 |
| IXIC | 纳斯达克综合指数 |
| NDX | 纳斯达克100指数 |
| SOX | 费城半导体指数 |
| N225 | 日经225指数 |
| HXC | 纳斯达克中国金龙指数 |

---

## backset()
本函数可能引入未来信息。若当前的x为True，则将从当前位置开到共n周期前的数值设为True，否则为False

### 句法
```python
backset(cond, n) -> Sequence[bool]
```

### 参数
**cond (Sequence(bool))** 判断条件，必填参数  
**n (int)** 周期数，必填参数

---

## bars_count()
返回第一个有效数据到当前的周期数

### 句法
```python
bars_count(x) -> Sequence[int]
```

### 参数
**x (Sequence([float, int, bool]))** 数据```Sequence```，必填参数

---

## bars_first_n()
对每一个数据点，计算周期n中第一次cond为True的点到该数据点的周期数，若该数据点之前周期n内cond均为False，则该数据点的值置为空值

### 句法
```python
bars_first_n(cond, n) -> Sequence[int]
```

### 参数
**cond (Sequence(bool))** 判断条件，必填参数  
**n (int)** 周期数，必填参数

---

## bars_last()
对每一个数据点，计算上一次cond为True的点到该数据点的周期数，若该数据点之前cond均为False，则该数据点的值置为空值

### 句法
```python
bars_last(cond) -> Sequence[int]
```

### 参数
**cond (Sequence(bool))** 判断条件，必填参数

---

## bars_last_count()
对每个数据点向前统计连续cond为True的周期数，返回最长的连续周期数

### 句法
```python
bars_last_count(cond) -> Sequence[float]
```

### 参数
**cond (Sequence(bool))** 判断条件，必填参数

---

## bars_next()
本函数可能引入未来信息。对每一个数据点，计算下一次cond为True的点到该数据点的周期数，若该数据点之后cond均为False，则该数据点的值置为空值

### 句法
```python
bars_next(cond) -> Sequence[int]
```

### 参数
**cond (Sequence(bool))** 判断条件，必填参数

---

## count()
返回n周期中满足cond=True的周期数

### 句法
```python
count(cond, n) -> Sequence[float]
```

### 参数
**cond (Sequence(bool))** 判断条件，必填参数  
**n (int)** 周期数，必填参数

---

## curr_bars_count()
返回每个时间点到最后的交易时间点的周期数

### 句法
```python
curr_bars_count(x) -> Sequence[int]
```

### 参数
**x (Sequence([float, int, bool]))** 数据```Sequence```，必填参数

---

## filter()
对某个数据点，若当前点的cond为True，将其后n周期内的数据置为False

### 句法
```python
filter(cond, n) -> Sequence[bool]
```

### 参数
**cond (Sequence(bool))** 判断条件，必填参数  
**n (int)** 周期数，必填参数

---

## find_n_highest()
返回在step步长前的n周期内的第top个最大值

### 句法
```python
find_n_highest(x, step, n, top) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**step (int)** 步长，可选参数，默认值为0  
**n (int)** 周期数，可选参数，默认值为1  
**top (int)** 位次，可选参数，默认值为1

---

## find_n_highest_bars()
对于每一个数据点，返回在step步长前的n周期内的第top个最大值所在位置到该数据点的周期数

### 句法
```python
find_n_highest_bars(x, step, n, top) -> Sequence[int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**step (int)** 步长，可选参数，默认值为0  
**n (int)** 周期数，可选参数，默认值为1  
**top (int)** 位次，可选参数，默认值为1

---

## find_n_lowest()
返回在step步长前的n周期内的第top个最小值

### 句法
```python
find_n_lowest(x, step, n, top) -> Sequence[float]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**step (int)** 步长，可选参数，默认值为0  
**n (int)** 周期数，可选参数，默认值为1  
**top (int)** 位次，可选参数，默认值为1

---

## find_n_lowest_bars()
对于每一个数据点，返回在step步长前的n周期内的第top个最小值所在位置到该数据点的周期数

### 句法
```python
find_n_lowest_bars(x, step, n, top) -> Sequence[int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**step (int)** 步长，可选参数，默认值为0  
**n (int)** 周期数，可选参数，默认值为1  
**top (int)** 位次，可选参数，默认值为1

---

## high_range()
返回每个点对应的值是过去多少个周期内的最大值

### 句法
```python
high_range(x) -> Sequence[int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数

---

## iff()
当logical为真时，取value_if_true对应的值，若为假，则取value_if_false对应的值

### 句法
```python
iff(logical,value_if_true,value_if_false) -> Sequence[int, float, bool, Color]
```

### 参数
**logical (Sequence(bool))** 判断结果，必填参数  
**value_if_true (Sequence([int, float,bool]),float,int, bool,Color, Shape)** 判断为True时的取值参考，必填参数  
**value_if_false (Sequence([int, float,bool,Color]),float,int, bool,Color)** 判断为False时的取值参考，必填参数

---

## low_range()
返回每个点对应的值是过去多少个周期内的最小值

### 句法
```python
low_range(x) -> Sequence[int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数

---

## sum_bars()
对于每个数据点，将之前的值向前累加，直到总和大于等于target，返回所用的值的个数；若之前所有的数据都累加后还不能达到target，则返回此数据点前面的数据点个数

### 句法
```python
sum_bars(x, target) -> Sequence[int]
```

### 参数
**x (Sequence([float, int]))** 数据```Sequence```，必填参数  
**target (float, int)** 目标值，必填参数

---

## value_when()
当logical为真时，取x对应值，若为假，则取上一次logical为真时的对应值

### 句法
```python
value_when(logical,x) -> Sequence[float]
```

### 参数
**logical (Sequence(bool))** 判断结果，必填参数  
**x (Sequence)** 取值参考，必填参数

---

## total_assets()
返回资产合计序列

### 句法
```python
total_assets(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY

| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## total_liabilities()
返回负债合计序列

### 句法
```python
total_liabilities(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY

| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## stockholders_equity()
返回归属于母公司所有者的股东权益合计序列

### 句法
```python
stockholders_equity(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## operating_cash_flow()
返回经营活动现金流量净额序列

### 句法
```python
operating_cash_flow(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## free_cash_flow()
返回自由现金流序列

### 句法
```python
free_cash_flow(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## total_revenue()
返回营业总收入序列

### 句法
```python
total_revenue(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY 


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## gross_profit()
返回毛利序列

### 句法
```python
gross_profit(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## operating_profit()
返回营业利润序列

### 句法
```python
operating_profit(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY

| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## net_profit()
返回净利润序列

### 句法
```python
net_profit(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## net_income_to_parent_company()
返回归母净利润序列

### 句法
```python
net_income_to_parent_company(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## eps_diluted()
返回稀释每股收益序列

### 句法
```python
eps_diluted(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## gross_margin()
返回毛利率序列，毛利率 = 毛利 / 营业总收入

### 句法
```python
gross_margin(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## ebit_to_revenue()
返回EBIT利润率序列，EBIT利润率 = EBIT / 营业收入

### 句法
```python
ebit_to_revenue(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## tax_rate()
返回税率序列

### 句法
```python
tax_rate(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## current_ratio()
返回流动比率序列，流动比率 = 流动资产 / 流动负债

### 句法
```python
current_ratio(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## quick_ratio()
返回速动比率序列，速动比率 = 速动资产 / 流动负债

### 句法
```python
quick_ratio(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## receivable_turnover()
返回应收账款周转率（次）序列，应收账款周转率 = 营业收入 / 平均应收账款余额

### 句法
```python
receivable_turnover(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## inventory_turnover()
返回存货周转率（次）序列，存货周转率 = 营业成本 / 平均存货余额

### 句法
```python
inventory_turnover(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## payable_turnover()
返回应付账款周转率（次）序列，应付账款周转率 = 营业成本 / 平均应付账款余额

### 句法
```python
payable_turnover(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## fixed_asset_turnover()
返回固定资产周转率（次）序列，固定资产周转率 = 营业收入 / 平均固定资产净值

### 句法
```python
fixed_asset_turnover(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## total_asset_turnover()
返回总资产周转率（次）序列，总资产周转率 = 营业收入 / 平均总资产

### 句法
```python
total_asset_turnover(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## return_on_equity()
返回净资产收益率（ROE）序列，ROE = 净利润 / 股东权益平均余额

### 句法
```python
return_on_equity(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## return_on_assets()
返回总资产净利率（ROA）序列，ROA = 净利润 / 平均总资产

### 句法
```python
return_on_assets(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## return_on_invested_capital()
返回投入资本回报率（ROIC）序列，ROIC = 税后净营业利润 / 平均投入资本

### 句法
```python
return_on_invested_capital(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY


| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## net_asset_per_share()
返回每股净资产序列，每股净资产 = 股东权益总额 / 普通股股数

### 句法
```python
net_asset_per_share(period) -> Sequence(float)
```

### 参数
**period (FinPeriod, str)** 财务报告类型，可选参数，默认值为FinPeriod.FQ，可能的值为'FQ','FY',FinPeriod.FQ,FinPeriod.FY

| 枚举值 | 含义 |
|--------|------|
| FQ / FinPeriod.FQ | 季度报告 |
| FY / FinPeriod.FY | 年度报告 |

---

## option_oi_pcr()
返回指定标的期权Put/Call总持仓量比

### 句法
```python
option_oi_pcr() -> Sequence(float)
```

---

## option_oi()
返回指定标的期权总持仓量

### 句法
```python
option_oi() -> Sequence(float)
```

---

## option_vol_pcr()
返回指定标的期权Put/Call总成交量比

### 句法
```python
option_vol_pcr() -> Sequence(float)
```

---

## option_vol()
返回指定标的期权总成交量

### 句法
```python
option_vol() -> Sequence(float)
```

---

## iv_percentile()
返回指定标的隐含波动率百分位数

### 句法
```python
iv_percentile() -> Sequence(float)
```

---

## iv_rank()
返回指定标的隐含波动率等级

### 句法
```python
iv_rank() -> Sequence(float)
```

---

## future_position_chg()
返回目标期货日增仓序列

### 句法
```python
future_position_chg() -> Sequence(float)
```

---

## future_oi()
返回目标期货持仓量序列

### 句法
```python
future_oi() -> Sequence(float)
```

---

## random.choice()
从非空序列 seq 返回一个随机元素

### 句法
```python
random.choice(seq) -> Any
```

### 参数
**seq (list, tuple, str)** 非空序列，必填参数

---

## random.randint()
返回随机整数 N 满足 a <= N <= b

### 句法
```python
random.randint(a, b) -> int
```

### 参数
**a (int, float)** 开始值，必填参数  
**b (int, float)** 结束值，必填参数

---

## random.random()
返回 0.0 <= X < 1.0 范围内的随机浮点数

### 句法
```python
random.random() -> float
```

---

## random.seed()
初始化随机数生成器

### 句法
```python
random.seed(a)
```

### 参数
**a (int, float, None)** 改变随机数生成器的种子，可选参数，默认值为None

---

## random.shuffle()
就地将序列 x 随机打乱位置

### 句法
```python
random.shuffle(seq) -> iterable
```

### 参数
**seq (list)** 列表，必填参数

---

## random.uniform()
返回一个随机浮点数 N ，当 a <= b 时 a <= N <= b ，当 b < a 时 b <= N <= a

### 句法
```python
random.uniform(a, b) -> float
```

### 参数
**a (int, float)** 随机数的下限，必填参数  
**b (int, float)** 随机数的上限，必填参数

---

## json.dumps()
将 Python 对象编码为 JSON 字符串

### 句法
```python
json.dumps(obj, indent) -> str
```

### 参数
**obj (object)** 要序列化的Python对象，必填参数  
**indent (int | str | None)** 缩进层级，可选参数，默认值为0

---

## json.loads()
将 JSON 字符串解码为 Python 对象

### 句法
```python
json.loads(data) -> object
```

### 参数
**data (str)** json字符串，必填参数

---

## abs()
返回一个数的绝对值

### 句法
```python
abs(x) -> Sequence[float, int] | float | int
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数

---

## callable()
如果参数 object 是可调用的就返回 True，否则返回 False

### 句法
```python
callable(object) -> bool
```

### 参数
**object (object)** Python对象，必填参数

---

## chr()
返回 Unicode 码位为整数 i 的字符的字符串格式

### 句法
```python
chr(i) -> str
```

### 参数
**i (int)** Unicode码位，必填参数

---

## delattr()
如果对象允许，该函数将删除指定的属性

### 句法
```python
delattr(object, name)
```

### 参数
**object (object)** Python对象，必填参数  
**name (str)** 属性名，必填参数

---

## divmod()
返回一个包含商和余数的元组(a // b, a % b)

### 句法
```python
divmod(a, b) -> Tuple[float, float]
```

### 参数
**a (int, float)** 被除数，必填参数  
**b (int, float)** 除数，必填参数

---

## getattr()
返回对象命名属性的值

### 句法
```python
getattr(object, name[, default]) -> Any
```

### 参数
**object (object)** Python对象，必填参数  
**name (str)** 属性名，必填参数

---

## globals()
以字典类型返回当前位置的全部全局变量

### 句法
```python
globals() -> dict
```

---

## hasattr()
如果字符串是对象的属性之一的名称，则返回 True，否则返回 False

### 句法
```python
hasattr(object, name) -> bool
```

### 参数
**object (object)** Python对象，必填参数  
**name (str)** 属性名，必填参数

---

## hash()
返回该对象的哈希值

### 句法
```python
hash(object) -> str | int
```

### 参数
**object (object)** Python对象，必填参数

---

## id()
返回对象的“标识值”

### 句法
```python
id(object) -> int
```

### 参数
**object (object)** Python对象，必填参数

---

## isinstance()
判断一个对象是否是一个已知的类型

### 句法
```python
isinstance(object, classinfo) -> bool
```

### 参数
**object (object)** Python对象，必填参数  
**classinfo (Union Type)** 目标，必填参数

---

## issubclass()
判断参数 class 是否是类型参数 classinfo 的子类

### 句法
```python
issubclass(class, classinfo) -> bool
```

### 参数
**class (class)** 类，必填参数  
**classinfo (class)** 类，必填参数

---

## iter()
返回一个 iterator 对象，sentinel 参数是可选的，当它存在时，object 不再传入一个可迭代对象，而是一个可调用对象，通俗点说就是可以通过()调用的对象，当可调用对象返回值为sentinel 时，循环结束

### 句法
```python
iter(object[, sentinel]) -> iterator
```

### 参数
**object (object)** Python对象，必填参数

---

## len()
返回对象的长度

### 句法
```python
len(s) -> int
```

### 参数
**s (object)** 可迭代对象，必填参数

---

## locals()
以字典类型返回当前位置的全部局部变量

### 句法
```python
locals() -> dict
```

---

## next()
获取下一个元素

### 句法
```python
next(iterator[, default]) -> object
```

### 参数
**iterator (object)** iterator对象，必填参数

---

## ord()
对表示单个 Unicode 字符的字符串，返回代表它 Unicode 码点的整数

### 句法
```python
ord(c) -> int
```

### 参数
**c (str)** 单个字符，必填参数

---

## round()
返回 x 舍入到小数点后 ndigits 位精度的值

### 句法
```python
round(x, ndigits) -> Sequence[float] | float
```

### 参数
**x (Sequence([float, int]), float, int)** 待计算的实参，必填参数  
**ndigits (int)** 保留的小数位数，可选参数，默认值为0

---

## setattr()
对应函数 getattr()，用于设置属性值，该属性不一定是存在的

### 句法
```python
setattr(object, name, value)
```

### 参数
**object (object)** Python对象，必填参数  
**name (str)** 属性名，必填参数  
**value (Union Type)** 属性值，必填参数

---

## hex()
将整数转换为以“0x”为前缀的小写十六进制字符串

### 句法
```python
hex(x) -> str
```

### 参数
**x (int)** 数值，必填参数

---

## BarType.K_1S
1秒K

---

## BarType.K_5S
5秒K

---

## BarType.K_10S
10秒K

---

## BarType.K_15S
15秒K

---

## BarType.K_30S
30秒K

---

## BarType.K_45S
45秒K

---

## BarType.K_1M
1分钟K

---

## BarType.K_3M
3分钟K

---

## BarType.K_5M
5分钟K

---

## BarType.K_10M
10分钟K

---

## BarType.K_15M
15分钟K

---

## BarType.K_30M
30分钟K

---

## BarType.K_60M
1小时K

---

## BarType.K_120M
2小时K

---

## BarType.K_180M
3小时K

---

## BarType.K_240M
4小时K

---

## BarType.K_DAY
日K

---

## BarType.K_WEEK
周K

---

## BarType.K_MON
月K

---

## BarType.K_QUARTER
季K

---

## BarType.K_YEAR
年K

---

## hex()
返回hex相应的颜色

### 句法
```python
Color.hex(hex_value) -> Color
```

### 参数
**hex_value (str)** 十六进制颜色值，必填参数

---

## rgb()
返回rgb相应的颜色

### 句法
```python
Color.rgb(r, g, b, a) -> Color
```

### 参数
**r (int)** 红色分量，必填参数，可能的值为0-255  
**g (int)** 绿色分量，必填参数，可能的值为0-255  
**b (int)** 蓝色分量，必填参数，可能的值为0-255  
**a (int)** 透明度分量，可选参数，默认值为255，可能的值为0-255

---

## Color.black
黑色

---

## Color.blue
蓝色

---

## Color.brown
棕色

---

## Color.cyan
青色

---

## Color.gray
灰色

---

## Color.green
绿色

---

## Color.liblue
淡蓝色

---

## Color.licyan
淡青色

---

## Color.ligray
淡灰色

---

## Color.ligreen
淡绿色

---

## Color.limagenta
淡洋红色

---

## Color.lired
淡红色

---

## Color.magenta
洋红色

---

## Color.red
红色

---

## Color.white
白色

---

## Color.yellow
黄色

---

## Color.up
上涨色

---

## Color.down
下跌色

---

## Line.circles
小圆圈线

---

## Line.cross
小叉线

---

## Line.histogram
柱形图

---

## Line.histogram_line
同时画出柱状线和指标线

---

## Line.line
直线

---

## Line.line_dashed
虚线

---

## Line.line_dotted
小圆点线

---

## Sequence.align()
将```Sequence```的时间索引对齐至base_seq

### 句法
```python
Sequence.align(base_seq) -> Sequence[float, int, bool]
```

### 参数
**base_seq(Sequence([float, int, bool]))** 目标时间索引序列，必填参数

---

## Sequence.apply()
对每一个元素运行指定的func

### 句法
```python
Sequence.apply(func) -> Sequence
```

### 参数
**func(function)** 函数，必填参数

---

## Sequence.avedev()
计算过去n个数与其算数平均值偏差绝对值的平均

### 句法
```python
Sequence.avedev(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.corr()
计算数据序列x和数据序列y过去n周期的相关系数

### 句法
```python
Sequence.corr(y, n) -> Sequence[float]
```

### 参数
**y (Sequence([float, int]))** 数据```Sequence```2，必填参数  
**n (int)** 周期数，必填参数

---

## Sequence.covar()
计算数据序列x和数据序列y过去n个周期的协方差

### 句法
```python
Sequence.covar(y, n) -> Sequence[float]
```

### 参数
**y (Sequence([float, int]))** 数据```Sequence```2，必填参数  
**n (int)** 周期数，必填参数

---

## Sequence.cross()
前n个点x均小于y，当前点x大于y, 则视为上穿，否则为不上穿

### 句法
```python
Sequence.cross(y, n) -> Sequence[bool]
```

### 参数
**y (Sequence([float, int]))** 数据```Sequence```2，必填参数  
**n (int)** 周期数，必填参数

---

## Sequence.devsq()
计算过去n个数与其算数平均值偏差的平方和

### 句法
```python
Sequence.devsq(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.diff()
计算当前元素和先前元素之间的差值

### 句法
```python
Sequence.diff(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，可选参数，默认值为1

---

## Sequence.dma()
计算动态移动平均

### 句法
```python
Sequence.dma(m) -> Sequence[float]
```

### 参数
**m (float)** 权重，必填参数

---

## Sequence.down_n_day()
过去n周期内的value均递减，则视为连跌，否则为不连跌

### 句法
```python
Sequence.down_n_day(n) -> Sequence[bool]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.ema()
计算指数移动平均

### 句法
```python
Sequence.ema(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，可选参数，默认值为5

---

## Sequence.exist()
过去n周期内的x至少出现过一个True，则视为存在，否则为不存在

### 句法
```python
Sequence.exist(n) -> Sequence[bool]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.fill_na()
判断每个数据点是否为空值，若为空值则按照指定方式（forward或backward）填充

### 句法
```python
Sequence.fill_na(method) -> Sequence[float, int, bool]
```

### 参数
**method (str)** 填充方法，可选参数，默认值为forward，可能的值为'forward','backward'

---

## Sequence.forcast()
以过去n个数与其对应顺序编号（从过去到现在第几根）进行带截距项的线性回归，通过拟合回归模型计算数据序列的下一期预测值

### 句法
```python
Sequence.forcast(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.greater_n_day()
过去n周期内的x均大于y，则视为连大，否则为不连大

### 句法
```python
Sequence.greater_n_day(y, n) -> Sequence[bool]
```

### 参数
**y (Sequence([float, int]))** 数据```Sequence```2，必填参数  
**n (int)** 周期数，必填参数

---

## Sequence.hhv()
求n周期内的最高值

### 句法
```python
Sequence.hhv(n, min_period) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## Sequence.hhv_bars()
对每一个数据点，返回n周期内最高值所在位置到当前数据点的周期数，若有多个相等的最高值，则返回距离当前数据点最近的位置

### 句法
```python
Sequence.hhv_bars(n, min_period) -> Sequence[int]
```

### 参数
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## Sequence.hod()
返回当前数据是n周期内的第几个高值

### 句法
```python
Sequence.hod(n, min_period) -> Sequence[int]
```

### 参数
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## Sequence.is_na()
判断每个数据点是否为空值，返回布尔值序列

### 句法
```python
Sequence.is_na() -> Sequence[bool]
```

---

## Sequence.last()
在step步长前n周期内x一直为True，则视为持续，否则为不持续

### 句法
```python
Sequence.last(step, n) -> Sequence[bool]
```

### 参数
**step (int)** 步长，必填参数  
**n (int)** 周期数，必填参数

---

## Sequence.llv()
求n周期内的最低值

### 句法
```python
Sequence.llv(n, min_period) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## Sequence.llv_bars()
对每一个数据点，返回n周期内最低值所在位置到当前数据点的周期数，若有多个相等的最低值，则返回距离当前数据点最近的位置

### 句法
```python
Sequence.llv_bars(n, min_period) -> Sequence[int]
```

### 参数
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## Sequence.lod()
返回当前数据是n周期内的第几个低值

### 句法
```python
Sequence.lod(n, min_period) -> Sequence[int]
```

### 参数
**n (int)** 周期数，必填参数  
**min_period (int)** 最小周期数，可选参数，默认值为1

---

## Sequence.max()
返回数据序列的最大值

### 句法
```python
Sequence.max() -> float | int
```

---

## Sequence.min()
返回数据序列的最小值

### 句法
```python
Sequence.min() -> float | int
```

---

## Sequence.mular()
计算n周期中x的乘积，n=0则从第一个非空值开始计算

### 句法
```python
Sequence.mular(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.pct_change()
计算当前元素和先前元素之间的百分比变化

### 句法
```python
Sequence.pct_change(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，可选参数，默认值为1

---

## Sequence.plot()
根据输入的序列绘制个性化的线型

### 句法
```python
Sequence.plot(name, color, style, linewidth)
```

### 参数
**name (str)** 名称，必填参数  
**color (Color, Sequence(Color))** 画线颜色，可选参数，默认值为Color.blue  
**style (Line)** 线型，可选参数，默认值为Line.line  
**linewidth (int)** 线宽，可选参数，默认值为1，可能的值为1~9

---

## Sequence.quantile()
计算分位数

### 句法
```python
Sequence.quantile(n) -> int | float
```

### 参数
**n (float)** 周期数，可选参数，默认值为0.5

---

## Sequence.ref()
引用n周期前的数据

### 句法
```python
Sequence.ref(n) -> Sequence[float, int, bool]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.replace_na()
判断每个数据点是否为空值，若为空值则填充为value

### 句法
```python
Sequence.replace_na(value) -> Sequence[float, int, bool]
```

### 参数
**value (int/float/bool)** 填充值，必填参数

---

## Sequence.reverse()
计算相反数

### 句法
```python
Sequence.reverse() -> Sequence[float, int]
```

---

## Sequence.slope()
以过去n个数与其对应顺序编号（从过去到现在第个数）进行带截距项的线性回归，获得回归模型对数据序列的拟合系数

### 句法
```python
Sequence.slope(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.sma()
计算简单移动平均

### 句法
```python
Sequence.sma(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，可选参数，默认值为5

---

## Sequence.smma()
计算平滑移动平均

### 句法
```python
Sequence.smma(n, m) -> Sequence[float]
```

### 参数
**n (int)** 周期数，可选参数，默认值为5  
**m (float)** 权重，可选参数，默认值为1

---

## Sequence.std()
计算过去n个数样本标准差

### 句法
```python
Sequence.std(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.stdp()
计算过去n个数总体标准差

### 句法
```python
Sequence.stdp(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.sum()
计算n周期中x的总和，n=0则从第一个非空值开始计算

### 句法
```python
Sequence.sum(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.sum_bars()
对于每个数据点，将之前的值向前累加，直到总和大于等于target，返回所用的值的个数；若之前所有的数据都累加后还不能达到target，则返回此数据点前面的数据点个数

### 句法
```python
Sequence.sum_bars(target) -> Sequence[int]
```

### 参数
**target (float, int)** 目标值，必填参数

---

## Sequence.tma()
计算递归移动平均

### 句法
```python
Sequence.tma(a, b) -> Sequence[float]
```

### 参数
**a (float)** 权重，可选参数，默认值为0.5  
**b (float)** 权重，可选参数，默认值为0.5

---

## Sequence.up_n_day()
过去n周期内的value均递增，则视为连涨，否则为不连涨

### 句法
```python
Sequence.up_n_day(n) -> Sequence[bool]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.var()
计算过去n个数样本方差

### 句法
```python
Sequence.var(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.varp()
计算过去n个数总体方差

### 句法
```python
Sequence.varp(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数

---

## Sequence.volat()
计算价格序列过去n个周期的年化波动率

### 句法
```python
Sequence.volat(n, bar_type) -> Sequence[float]
```

### 参数
**n (int)** 周期数，必填参数  
**bar_type (BarType)** 频率，可选参数，默认值为None

---

## Sequence.wma()
计算加权移动平均

### 句法
```python
Sequence.wma(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，可选参数，默认值为5

---

## Sequence.xma()
计算偏移移动平均

### 句法
```python
Sequence.xma(n) -> Sequence[float]
```

### 参数
**n (int)** 周期数，可选参数，默认值为5

---

## Sequence.size
返回数据序列中的元素数

---

## Sequence.values
返回数据序列的值

### 句法
```python
Sequence.values -> List
```

---

## Shape.arrowdown
箭头向下

---

## Shape.arrowup
箭头向上

---

## Shape.circle
圆形

---

## Shape.cross
十字形状

---

## Shape.diamond
钻石形状

---

## Shape.flag
标志

---

## Shape.labeldown
标签向下

---

## Shape.labelup
标签向上

---

## Shape.square
正方形

---

### Shape.triangledown
三角形向下

---

## Shape.triangleup
三角形向上

---

## Shape.xcross
交叉形状

---

## Time()
初始化Time类对象

### 句法
```python
Time(year, month, day, hour, minute, second, microsecond, tz, market) -> Time
```

### 参数
**year (int)** 年，必填参数  
**month (int)** 月，必填参数  
**day (int)** 日，必填参数  
**hour (int)** 时，可选参数，默认值为0  
**minute (int)** 分，可选参数，默认值为0  
**second (int)** 秒，可选参数，默认值为0  
**microsecond (int)** 微秒，可选参数，默认值为0  
**tz (str)** 时区，必填参数  
**market (str)** 市场，可选参数，默认值为None

---

## astimezone()
将Time对象的时区进行转换

### 句法
```python
Time.astimezone(tz) -> Time
```

### 参数
**tz (str)** 时区，必填参数

---

## Time.day()
返回Time对象的日期

### 句法
```python
Time.day() -> int
```

---

## Time.fromisoformat()
解析以ISO8601格式给出的字符串所对应的日期

### 句法
```python
Time.fromisoformat(s,tz) -> Time
```

### 参数
**s (str)** 代表日期的字符串，必填参数  
**tz (str)** 时区，必填参数

---

## Time.hour()
返回Time对象的小时数

### 句法
```python
Time.hour() -> int
```

---

## Time.minute()
返回Time对象的分钟数

### 句法
```python
Time.minute() -> int
```

---

## Time.month()
返回Time对象的月份

### 句法
```python
Time.month() -> int
```

---

## Time.now()
返回指定时区的当前时间

### 句法
```python
Time.now() -> Time
```

---

## Time.second()
返回Time对象的秒数

### 句法
```python
Time.second() -> int
```

---

## Time.strftime()
自定义Time对象的格式化输出

### 句法
```python
Time.strftime(format) -> Time
```

### 参数
**format (str)** 指定格式，必填参数

---

## Time.weekday()
返回Time对象所处是星期几

### 句法
```python
Time.weekday() -> int
```

---

## Time.weekofyear()
返回Time对象所处周是年内第几个周

### 句法
```python
Time.weekofyear() -> int
```

---

## Time.year()
返回Time对象的年份

### 句法
```python
Time.year() -> int
```

---

## Time.tzinfo
返回时区

---

## TimeDelta()
初始化TimeDelta类对象

### 句法
```python
TimeDelta(weeks, days, hours, seconds, milliseconds, microseconds) -> TimeDelta
```

### 参数
**weeks (int)** 星期数，可选参数，默认值为0  
**days (int)** 日数，可选参数，默认值为0  
**hours (int)** 小时数，可选参数，默认值为0  
**seconds (int)** 秒数，可选参数，默认值为0  
**milliseconds (int)** 毫秒数，可选参数，默认值为0  
**microseconds (int)** 微秒数，可选参数，默认值为0

---

## TimeDelta.days
天数

---

## TimeDelta.seconds
秒数

---

## FinPeriod.FQ
单季报

---

## FinPeriod.FY
年报

---

## MoneyFlowType.extra_large
特大单

---

## MoneyFlowType.large
大单

---

## MoneyFlowType.medium
中单

---

## MoneyFlowType.small
小单

---

## math.e
数学常数 e = 2.718281828459045，精确到可用精度

---

## math.inf
浮点正无穷大。对于负无穷大，使用 -math.inf 。相当于 float('inf') 的输出

---

## math.nan
浮点数 "Not a Number" (NaN)。 相当于 float('nan') 的输出

---

## math.pi
数学常数 π = 3.141592653589793，精确到可用精度

---

<meta no_market>

## and
逻辑运算符

---

<meta no_market>

## as
创建别名

---

<meta no_market>

## assert
用于调试

---

<meta no_market>

## break
跳出循环

---

<meta no_market>

## class
定义类

---

<meta no_market>

## continue
继续循环的下一个迭代

---

<meta no_market>

## def
定义函数

---

<meta no_market>

## del
删除对象

---

<meta no_market>

## elif
在条件语句中使用，等同于 else if

---

<meta no_market>

## else
用于条件语句

---

<meta no_market>

## except
处理异常，发生异常时如何执行

---

<meta no_market>

## False 
布尔值，比较运算的结果

---

<meta no_market>

## finally
处理异常，无论是否存在异常，都将执行一段代码

---

<meta no_market>

## for
创建 for 循环

---

<meta no_market>

## from
导入模块的特定部分

---

<meta no_market>

## global
声明全局变量

---

<meta no_market>

## if
写一个条件语句

---

<meta no_market>

## import
导入模块

---

<meta no_market>

## in
检查列表、元组等集合中是否存在某个值

---

<meta no_market>

## is
测试两个变量是否相等

---

<meta no_market>

## lambda
创建匿名函数

---

<meta no_market>

## None
表示 null 值

---

<meta no_market>

## nonlocal
声明非局部变量

---

<meta no_market>

## not
逻辑运算符

---

<meta no_market>

## or
逻辑运算符

---

<meta no_market>

## pass
null 语句，一条什么都不做的语句

---

<meta no_market>

## raise
产生异常

---

<meta no_market>

## return
退出函数并返回值

---

<meta no_market>

## True 
布尔值，比较运算的结果

---

<meta no_market>

## try
编写 try...except 语句

---

<meta no_market>

## while
创建 while 循环

---

<meta no_market>

## with
用于简化异常处理

---

<meta no_market>

## yield
结束函数，返回生成器

---

## int
整型数

---

## float
浮点数

---

## complex
复数

---

## bool
布尔值

---

## str
字符串

---

## list
列表

---

## dict
字典

---

## set
集合

---

## tuple
元组

---

## fbasic.Sequence
数据序列类

---

## fbasic.BarType
时间周期类

---

## fplot.Color
颜色类

---

## fplot.Line
线性枚举类

---

## fplot.Shape
形状枚举类

---

## fdatetime.Time
时间类

---

## fdatetime.TimeDelta
时间差类

---

## ffinance.FinPeriod
财报周期类

---

## fquote.MoneyFlowType
资金流向类

---

<meta no_market>

## +
加

### 句法
```python
x + y
```

---

<meta no_market>

## +=
加法赋值

### 句法
```python
x += y
```

---

<meta no_market>

## -
减/取相反数

### 句法
```python
x - y
-x
```

---

<meta no_market>

## -=
减法赋值

### 句法
```python
x -= y
```

---

<meta no_market>

## *
乘

### 句法
```python
x * y
```

---

<meta no_market>

## *=
乘法赋值

### 句法
```python
x *= y
```

---

<meta no_market>

## /
除

### 句法
```python
x / y
```

---

<meta no_market>

## /=
除法赋值

### 句法
```python
x /= y
```

---

<meta no_market>

## //
取整除

### 句法
```python
x // y
```

---

<meta no_market>

## //=
取整除赋值

### 句法
```python
x //= y
```

---

<meta no_market>

## %
取模

### 句法
```python
x % y
```

---

<meta no_market>

## %=
取模赋值

### 句法
```python
x %= y
```

---

<meta no_market>

## **
幂

### 句法
```python
x ** y
```

---

<meta no_market>

## **=
幂赋值

### 句法
```python
x **= y
```

---

<meta no_market>

## =
赋值

### 句法
```python
x = a
```

---

<meta no_market>

## <
小于

### 句法
```python
x < y
```

---

<meta no_market>

## >
大于

### 句法
```python
x > y
```

---

<meta no_market>

## <=
小于等于

### 句法
```python
x <= y
```

---

<meta no_market>

## >=
大于等于

### 句法
```python
x >= y
```

---

<meta no_market>

## ==
等于

### 句法
```python
x == y
```

---

<meta no_market>

## !=
不等于

### 句法
```python
x != y
```

---

<meta no_market>

## &
按位与运算符

### 句法
```python
x & y
```

---

<meta no_market>

## |
按位或运算符

### 句法
```python
x | y
```

---

<meta no_market>

## ~
按位非运算符

### 句法
```python
~x
```

---

<meta no_market>

## &
按位异或运算符

### 句法
```python
x ^ y
```

---

<meta no_market>

## <<
左移动运算符

### 句法
```python
x << a
```

---

<meta no_market>

## >>
右移动运算符

### 句法
```python
x >> a
```

---

# THType

时段类型

| 枚举值 | 枚举说明 |
|-----|-----|
| RTH | 盘中 |
| ETH | 盘中+盘前盘后 |
| ALL | 全时段 |

---

# BarType

K线周期

| 枚举值 | 枚举说明 |
|-----|-----|
| M1 | 1分K |
| M3 | 3分K（暂不支持期权）|
| M5 | 5分K |
| M10 | 10分K（暂不支持期权）|
| M15 | 15分K|
| M30 | 30分K（暂不支持期权）|
| H1 | 1小时K |
| H2 | 2小时K（暂不支持期权）|
| H3 | 3小时K（暂不支持期权）|
| H4 | 4小时K（暂不支持期权）|
| D1 | 日K |
| W1 | 周K（暂不支持期权）|

---

# BarDataType

数据类型

| 枚举值 | 枚举说明 |
|-----|-----|
| CLOSE | 收盘价 |
| OPEN | 开盘价 |
| HIGH | 最高价 |
| LOW | 最低价 |
| VOLUME | 成交量 |
| TURNOVER | 成交额 |
| TURNOVER_RATE | 换手率 |
| CHG_RATE | 涨跌幅 |
| CHG | 涨跌额 |

---

# IndexOptionType

指数期权类型

| 枚举值 | 枚举说明 |
|-----|-----|
| NORMAL | 普通的指数期权 |
| SMALL | 小型指数期权 |

---

# DataType

数据类型

| 枚举值 | 枚举说明 |
|-----|-----|
| CLOSE | 收盘价 |
| OPEN | 开盘价 |
| HIGH | 最高价 |
| LOW | 最低价 |
| VOLUME | 成交量 |

---

# DealStatus

成交状态

| 枚举值 | 枚举说明 |
|-----|-----|
| OK | 正常 |
| CANCELLED | 成交被取消 |
| CHANGED | 成交被更改 |

---

# CltRiskStatus

风险状态

| 枚举值 | 枚举说明 |
|-----|-----|
| LEVEL1 | 非常安全 |
| LEVEL2 | 安全 |
| LEVEL3 | 较安全 |
| LEVEL4 | 较低风险 |
| LEVEL5 | 中等风险 |
| LEVEL6 | 偏高风险 |
| LEVEL7 | 预警 |
| LEVEL8 | 危险 |
| LEVEL9 | 危险 |

---

# OptionType

期权看涨/看跌类型

| 枚举值 | 枚举说明 |
|-----|-----|
| ALL | 所有 |
| CALL | 看涨期权 |
| PUT | 看跌期权 |

---

# OrdType

订单类型

| 枚举值 | 枚举说明 |
|-----|-----|
| LMT | 限价单 |
| MKT | 市价单 |
| STOP_LMT | 止损限价单 |
| STOP | 止损市价单 |
| LIM_IF_TOUCHED | 触及限价单（止盈） |
| MKT_IF_TOUCHED | 触及市价单（止盈） |
| TRAILING_STOP_LMT | 跟踪止损限价单 |
| TRAILING_STOP | 跟踪止损市价单 |

---

# PositionSide

持仓方向

| 枚举值 | 枚举说明 |
|-----|-----|
| LONG | 开多 |
| SHORT | 开空 |
| NONE | 无持仓 |

---

# Currency

货币

| 枚举值 | 枚举说明 |
|-----|-----|
| HKD | 港元 |
| USD | 美元 |
| CNH | 离岸人民币 |
| JPY | 日元 |
| SGD | 新元 |
| AUD | 澳元 |

---

# TimeInForce

订单期限

| 枚举值 | 枚举说明 |
|-----|-----|
| DAY | 当日有效 |
| GTC | 撤单前有效 |

---

# Week

周

| 枚举值 | 枚举说明 |
|-----|-----|
| MON | 周一 |
| TUE | 周二 |
| WED | 周三 |
| THU | 周四 |
| FRI | 周五 |
| SAT | 周六 |
| SUN | 周日 |

---

# Moneyness

期权价内/价外类型

| 枚举值 | 枚举说明 |
|-----|-----|
| ITM | 价内 |
| OTM | 价外 |

---

# TimeZone

时区

| 枚举值 | 枚举说明 |
|-----|-----|
| DEVICE_TIME_ZONE | 本机时区 |
| MARKET_TIME_ZONE | 标的所属市场时区（仅适用于[订单创建时间](232512247364)与[成交时间](237141247365)接口） |
| ET | 美国东部时间 |
| CT | 美国中部时间 |
| HST | 夏威夷时间 |
| AKST | 阿拉斯加时间 |
| PST | 太平洋时间 |
| MST | 美国山地时间 |
| CCT | 北京时间 |
| GMT | 英国时间 |
| CET | 中欧时间 |
| EET | 东欧时间 |
| JST | 日本时间 |
| KST | 韩国时间 |
| AET | 悉尼时间 |
| UTC_MINUS_11 | UTC-11 |
| UTC_MINUS_10 | UTC-10 |
| UTC_MINUS_9 | UTC-9 |
| UTC_MINUS_8 | UTC-8 |
| UTC_MINUS_7 | UTC-7 |
| UTC_MINUS_6 | UTC-6 |
| UTC_MINUS_5 | UTC-5 |
| UTC_MINUS_4 | UTC-4 |
| UTC_MINUS_3 | UTC-3 |
| UTC_MINUS_2 | UTC-2 |
| UTC_MINUS_1 | UTC-1 |
| UTC | UTC |
| UTC_PLUS_1 | UTC+1 |
| UTC_PLUS_2 | UTC+2 |
| UTC_PLUS_3 | UTC+3 |
| UTC_PLUS_4 | UTC+4 |
| UTC_PLUS_5 | UTC+5 |
| UTC_PLUS_6 | UTC+6 |
| UTC_PLUS_7 | UTC+7 |
| UTC_PLUS_8 | UTC+8 |
| UTC_PLUS_9 | UTC+9 |
| UTC_PLUS_10 | UTC+10 |
| UTC_PLUS_11 | UTC+11 |
| UTC_PLUS_12 | UTC+12 |

---

# CustomType

自定义周期

| 枚举值 | 枚举说明 |
|-----|-----|
| M1 | 分K |
| H1 | 小时K |
| D1 | 日K |

---

# TradeSide

交易方向

| 枚举值 | 枚举说明 |
|-----|-----|
| BUY | 买 |
| SELL | 卖 |
| ALL | 全部 |

---

# OrderStatus

订单状态

| 枚举值 | 枚举说明 |
|-----|-----|
| WAITING_SUBMIT | 待提交 |
| SUBMITTING | 提交中 |
| SUBMITTED | 已提交，等待成交 |
| FILLED_PART | 部分成交 |
| FILLED_ALL | 全部已成交 |
| CANCELLED_PART | 部分成交，剩余部分已撤单 |
| CANCELLED_ALL | 全部已撤单，无成交 |
| FAILED | 下单失败，服务拒绝 |
| DISABLED | 已失效 |

---

# TrdHours

交易时段

| 枚举值 | 枚举说明 |
|-----|-----|
| RTH | 盘中交易时段 |
| ITH | 非盘中交易时段 |
| CLOSED | 收盘时段 |

---

# TrailType

跟踪类型

| 枚举值 | 枚举说明 |
|-----|-----|
| RATIO | 比例 |
| AMOUNT | 金额 |

---

# TimeOrientation

时间判断方向

| 枚举值 | 枚举说明 |
|-----|-----|
| LATER_THAN | 晚于 |
| EARLIER_THAN | 早于 |
| NOT_LATER_THAN | 不晚于 |
| NOT_EARLIER_THAN | 不早于 |

---

# InlinePriceType

类型

| 枚举值 | 枚举说明 |
|-----|-----|
| UPPER_LIMIT | 上限 |
| LOWER_LIMIT | 下限 |

---

# OptionClass

分类方式

| 枚举值 | 枚举说明 |
|-----|-----|
| Moneyness | 价值状态 |
| Type | 方向 |
| Style | 行权时间 |

---

# DTStatus

日内交易限制情况

| 枚举值 | 枚举说明 |
|-----|-----|
| UNLIMITED | 无限次 |
| EM_Call | EM_Call |
| DT_Call | DT_Call |

---

# OptionCategory

期权类型

| 枚举值 | 枚举说明 |
|-----|-----|
| ITM | 价内 |
| OTM | 价外 |
| CALL | 看涨期权 |
| PUT | 看跌期权 |
| AMERICAN | 美式期权 |
| EUROPEAN | 欧式期权 |
| BERMUDA | 百慕大期权 |

---

# OrderSide

订单方向

| 枚举值 | 枚举说明 |
|-----|-----|
| BUY | 买入 |
| SELL | 卖出 |
| SELL_SHORT | 卖空 |
| BUY_BACK | 买回 |

---

# GlobalType

全局变量类型

| 枚举值 | 枚举说明 |
|-----|-----|
| FLOAT | 浮点数 |
| INT | 整数 |
| BOOL | 布尔值 |

---

# CostPriceModel

成本价模式

| 枚举值 | 枚举说明 |
|-----|-----|
| DILUTED | 摊薄成本价 |
| AVG | 平均成本价 |

---

# FutureType

期货合约类型

| 枚举值 | 枚举说明 |
|-----|-----|
| ALL | 所有期货合约 |
| MAIN | 主连期货合约 |
| CURRENT | 当月期货合约 |
| NEXT | 下月期货合约 |
| DAY | 仅日市期货合约 |
| MONTH | 月份期货合约 |

---

# MktStatus

市场状态

| 枚举值 | 枚举说明 |
|-----|-----|
| AUCTION | 竞价时段 |
| CONTINUOUS_TRADE | 持续交易时段 |
| CLOSED | 收盘时段 |

---

# USMktStatus

美股市场状态

| 枚举值 | 枚举说明 |
|-----|-----|
| PRE_MARKET | 盘前交易时段 |
| RTH | 盘中交易时段 |
| POST_MARKET | 盘后交易时段 |
| OVERNIGHT | 夜盘交易时段 |
| CLOSED | 收盘时段 |

---

# TSType

交易时段

| 枚举值 | 枚举说明 |
|-----|-----|
| ALL | 全时段 |
| RTH | 盘中 |
| ETH | 盘中+盘前盘后 |
| OVERNIGHT | 仅夜盘 |
| AUTO | 自动（已废弃） |

---

# AlgoStrategyType

策略类型

| 枚举值 | 枚举说明 |
|-----|-----|
| SECURITY | 证券 |
| FUTURE | 期货 |
